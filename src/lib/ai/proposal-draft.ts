// Anthropic Messages API call + JSON parsing for MoM → proposal draft.
// Must enforce quota checks before calling the API, and must never pipe
// model output directly to PDF generation (CLAUDE.md §6, known constraint 8).
// Wired in the backend integration phase.
export {};
