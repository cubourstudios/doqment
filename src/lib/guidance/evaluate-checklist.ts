// Pure function evaluating which documents a project needs, and why —
// the core mechanic described in docs/content-strategy.md §3. Implemented
// against real rules in Build Sequence step D4; unit-tested in tests/.
export {};
