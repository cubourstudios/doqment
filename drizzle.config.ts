// Drizzle Kit config, run against DIRECT_DATABASE_URL (CLAUDE.md §6, known
// constraint 4). Wired in the backend integration phase (CLAUDE.md §7, D7)
// once drizzle-orm/drizzle-kit are installed per the Section 4 package manifest.
export {};
